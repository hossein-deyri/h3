import styled from "styled-components";

export const CustomEditorStyles = styled.div`
  direction: ltr;

  .ql-container {
    height: 320px;
    border-radius: 0 0 0.25rem 0.25rem;
  }

  .ql-editor {
    direction: rtl;
    text-align: right;
  }

  .ql-toolbar {
    border-radius: 0.25rem 0.25rem 0 0;
  }
`;
